"use client";

// ChatWidget is no longer used — chat is now rendered full-page via AppShell.
// Kept as empty export to avoid breaking any lingering imports.
export function ChatWidget() {
  return null;
}
